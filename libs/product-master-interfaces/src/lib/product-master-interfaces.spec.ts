import { productMasterInterfaces } from './product-master-interfaces';

describe('productMasterInterfaces', () => {
  it('should work', () => {
    expect(productMasterInterfaces()).toEqual('product-master-interfaces');
  });
});
